import { test } from '@playwright/test';

test.describe('Coursework flows', () => {
  test('Student uploads submission; Educator grades with rubric', async () => {
    test.skip(true, 'Implement once coursework workflows are wired up');
  });
});
