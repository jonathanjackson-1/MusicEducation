import { test } from '@playwright/test';

test.describe('Practice timer resilience', () => {
  test('Practice timer offline → sync, streak updates', async () => {
    test.skip(true, 'Implement once offline sync logic is testable');
  });
});
